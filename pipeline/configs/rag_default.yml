data_root: "rag-pipeline/data/raw"

chunking:
  strategy: "fixed_window"
  size: 500
  overlap: 50

embeddings:
  backend: "openai"
  model_name: "text-embedding-3-small"

vectorstore:
  backend: "faiss"

rag:
  top_k: 5


